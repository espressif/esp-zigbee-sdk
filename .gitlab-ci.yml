workflow:
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: never
    - when: always

include:
  - '.gitlab/ci/common.yml'
  - '.gitlab/ci/sync_test_branch.yml'
  - '.gitlab/ci/generate_build_test_jobs.yml'
  - '.gitlab/ci/docs.yml'
  - '.gitlab/ci/check_typos.yml'
